import { FC, memo } from 'react';
import AgentPropertiesActivity from 'app/modules/_secured/AgentProperties';

const AgentProperties: FC = () => {
  return <AgentPropertiesActivity />;
};

export default memo(AgentProperties);
